name: Build APK

on:
  push:
    branches: [ "**" ]
  pull_request:
    branches: [ "**" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: gradle

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Accept SDK licenses
        run: yes | sdkmanager --licenses || true

      - name: Generate Xposed stub JAR
        run: |
          mkdir -p app/libs
          mkdir -p /tmp/xstub/de/robv/android/xposed/callbacks

          cat > /tmp/xstub/de/robv/android/xposed/IXposedHookLoadPackage.java << 'JAVA'
          package de.robv.android.xposed;
          import de.robv.android.xposed.callbacks.XC_LoadPackage;
          public interface IXposedHookLoadPackage {
              void handleLoadPackage(XC_LoadPackage.LoadPackageParam lpparam) throws Throwable;
          }
          JAVA

          cat > /tmp/xstub/de/robv/android/xposed/XposedBridge.java << 'JAVA'
          package de.robv.android.xposed;
          public class XposedBridge {
              public static void log(String text) {}
              public static void log(Throwable t) {}
              public static Object invokeOriginalMethod(java.lang.reflect.Member method,
                  Object thisObject, Object[] args) throws Throwable { return null; }
          }
          JAVA

          cat > /tmp/xstub/de/robv/android/xposed/XposedHelpers.java << 'JAVA'
          package de.robv.android.xposed;
          import java.lang.reflect.*;
          public class XposedHelpers {
              public static Object findAndHookMethod(String className, ClassLoader cl,
                  String methodName, Object... args) { return null; }
              public static Object findAndHookMethod(Class<?> clazz,
                  String methodName, Object... args) { return null; }
              public static Object getObjectField(Object obj, String fieldName) { return null; }
              public static void setObjectField(Object obj, String fieldName, Object value) {}
              public static int getIntField(Object obj, String fieldName) { return 0; }
              public static void setIntField(Object obj, String fieldName, int value) {}
              public static boolean getBooleanField(Object obj, String fieldName) { return false; }
              public static void setBooleanField(Object obj, String fieldName, boolean value) {}
              public static Object callMethod(Object obj, String methodName, Object... args) { return null; }
              public static Object callStaticMethod(Class<?> clazz, String methodName, Object... args) { return null; }
              public static Class<?> findClass(String className, ClassLoader classLoader) { return null; }
              public static Object newInstance(Class<?> clazz, Object... args) { return null; }
          }
          JAVA

          cat > /tmp/xstub/de/robv/android/xposed/XC_MethodHook.java << 'JAVA'
          package de.robv.android.xposed;
          public abstract class XC_MethodHook {
              public void beforeHookedMethod(MethodHookParam param) throws Throwable {}
              public void afterHookedMethod(MethodHookParam param) throws Throwable {}
              public class MethodHookParam {
                  public java.lang.reflect.Member method;
                  public Object thisObject;
                  public Object[] args;
                  private Object result;
                  private Throwable throwable;
                  public Object getResult() { return result; }
                  public void setResult(Object result) { this.result = result; }
                  public Throwable getThrowable() { return throwable; }
                  public boolean hasThrowable() { return throwable != null; }
                  public Object getResultOrThrowable() throws Throwable {
                      if (throwable != null) throw throwable; return result;
                  }
              }
          }
          JAVA

          cat > /tmp/xstub/de/robv/android/xposed/callbacks/XC_LoadPackage.java << 'JAVA'
          package de.robv.android.xposed.callbacks;
          public class XC_LoadPackage {
              public static class LoadPackageParam {
                  public String packageName;
                  public String processName;
                  public ClassLoader classLoader;
                  public android.content.pm.ApplicationInfo appInfo;
                  public boolean isFirstApplication;
              }
          }
          JAVA

          cat > /tmp/xstub/de/robv/android/xposed/IXposedHookZygoteInit.java << 'JAVA'
          package de.robv.android.xposed;
          public interface IXposedHookZygoteInit {
              void initZygote(StartupParam startupParam) throws Throwable;
              class StartupParam { public String modulePath; }
          }
          JAVA

          cd /tmp/xstub
          find . -name "*.java" | xargs javac -source 8 -target 8 2>/dev/null || true
          jar cf /tmp/XposedBridgeApi.jar $(find . -name "*.class")
          cp /tmp/XposedBridgeApi.jar /home/runner/work/vcamsx1/vcamsx1/app/libs/XposedBridgeApi.jar
          echo "Xposed stub: $(wc -c < /home/runner/work/vcamsx1/vcamsx1/app/libs/XposedBridgeApi.jar) bytes"

      - name: Download IjkPlayer JARs
        run: |
          mkdir -p app/libs

          curl -fL \
            "https://repo1.maven.org/maven2/tv/danmaku/ijk/media/ijkplayer-java/0.8.8/ijkplayer-java-0.8.8.jar" \
            -o app/libs/ijkplayer-java.jar
          echo "IjkJava: $(wc -c < app/libs/ijkplayer-java.jar) bytes"

          curl -fL \
            "https://repo1.maven.org/maven2/tv/danmaku/ijk/media/ijkplayer-arm64/0.8.8/ijkplayer-arm64-0.8.8.aar" \
            -o app/libs/ijkplayer-arm64.aar
          echo "IjkArm64: $(wc -c < app/libs/ijkplayer-arm64.aar) bytes"

          ls -lh app/libs/

      - name: Create debug keystore
        run: |
          mkdir -p keystore
          keytool -genkey -v \
            -keystore keystore/debug.keystore \
            -storepass android \
            -alias androiddebugkey \
            -keypass android \
            -keyalg RSA -keysize 2048 -validity 10000 \
            -dname "CN=Android Debug,O=Android,C=US"

      - name: Grant gradlew execute
        run: chmod +x gradlew

      - name: Build Debug APK
        run: ./gradlew assembleDebug --no-daemon --stacktrace 2>&1 | tail -60

      - name: Upload APK
        uses: actions/upload-artifact@v4
        if: success()
        with:
          name: VCam-debug
          path: app/build/outputs/apk/debug/app-debug.apk
          retention-days: 30
